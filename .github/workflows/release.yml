name: release

on:
  push:
    tags:
      - "v*.*.*"

permissions:
  contents: write

jobs:
  build:
    strategy:
      fail-fast: false
      matrix:
        include:
          - name: macos-arm64
            platform: macos-latest
            args: --target aarch64-apple-darwin
            verify_platform: macos
            target: aarch64-apple-darwin
            bundle_path: src-tauri/target/aarch64-apple-darwin/release/bundle/**
          - name: windows-x64
            platform: windows-latest
            args: ""
            verify_platform: windows
            target: ""
            bundle_path: src-tauri/target/release/bundle/**

    runs-on: ${{ matrix.platform }}

    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v4
        with:
          version: 10.34.3

      - uses: actions/setup-node@v4
        with:
          node-version-file: .node-version
          cache: pnpm

      - uses: dtolnay/rust-toolchain@stable

      - name: Cache Cargo registry and build
        uses: actions/cache@v6
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            src-tauri/target
          key: ${{ runner.os }}-cargo-${{ hashFiles('src-tauri/Cargo.lock') }}
          restore-keys: ${{ runner.os }}-cargo-

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Pre-flight checks
        run: pnpm release:smoke

      - name: Build Tauri bundle
        run: pnpm tauri build ${{ matrix.args }}
        env:
          TAURI_SIGNING_PRIVATE_KEY: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY }}
          TAURI_SIGNING_PRIVATE_KEY_PASSWORD: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY_PASSWORD }}

      - name: Verify release artifacts (no target)
        if: matrix.target == ''
        run: pnpm release:verify-artifacts -- --platform ${{ matrix.verify_platform }}

      - name: Verify release artifacts (with target)
        if: matrix.target != ''
        run: pnpm release:verify-artifacts -- --platform ${{ matrix.verify_platform }} --target ${{ matrix.target }}

      - uses: actions/upload-artifact@v4
        with:
          name: tunnel-mcp-${{ matrix.name }}
          path: ${{ matrix.bundle_path }}
          if-no-files-found: error

  publish:
    needs: build
    runs-on: ubuntu-latest

    steps:
      - uses: actions/download-artifact@v4
        with:
          path: release-artifacts
          merge-multiple: true

      - name: Publish GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          files: release-artifacts/**/*
          generate_release_notes: true